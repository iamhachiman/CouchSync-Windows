<Window x:Class="CouchSync.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:CouchSync"
        mc:Ignorable="d"
        Title="CouchSync 🛋️📲" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid>
        <!-- PAIRING SCREEN -->
        <Grid x:Name="PairingScreen" Visibility="Visible">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Welcome to CouchSync" FontSize="32" FontWeight="SemiBold" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                <TextBlock Text="Scan this QR Code from the CouchSync Android App to connect." Foreground="{DynamicResource MaterialDesignBodyLight}" FontSize="16" HorizontalAlignment="Center" Margin="0,0,0,30"/>
                
                <Border Background="White" Padding="20" CornerRadius="16" HorizontalAlignment="Center" Effect="{DynamicResource MaterialDesignElevationShadow4}">
                    <Image x:Name="QrCodeImage" Width="250" Height="250" Stretch="Uniform"/>
                </Border>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30,0,0">
                    <TextBlock Text="Or enter this pin manually: " FontSize="18" Foreground="{DynamicResource MaterialDesignBodyLight}" VerticalAlignment="Center"/>
                    <TextBlock x:Name="PairingCodeText" Text="----" FontSize="26" FontWeight="Black" Foreground="{DynamicResource PrimaryHueMidBrush}" VerticalAlignment="Center"/>
                </StackPanel>
                
                <TextBlock x:Name="NetworkStatusText" Text="Waiting for connection..." Margin="0,20,0,0" TextWrapping="Wrap" TextAlignment="Center" FontSize="14" Foreground="{DynamicResource PrimaryHueLightBrush}" />
            </StackPanel>
        </Grid>

        <!-- MAIN SCREEN (PHONE LINK STYLE) -->
        <Grid x:Name="MainScreen" Visibility="Collapsed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <Border Grid.Column="0" Background="{DynamicResource MaterialDesignPaper}" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,1,0">
                <StackPanel Margin="20">
                    <TextBlock Text="CouchSync" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0,0,0,30"/>
                    
                    <materialDesign:Card Padding="15" UniformCornerRadius="10" Background="{DynamicResource PrimaryHueLightBrush}" Foreground="{DynamicResource PrimaryHueLightForegroundBrush}">
                        <StackPanel>
                            <TextBlock Text="📱 Connected Device" FontSize="14" Margin="0,0,0,5" Opacity="0.8"/>
                            <TextBlock Text="Android Phone" FontSize="18" FontWeight="SemiBold"/>
                            <TextBlock Text="Status: Active &amp; Relaying" FontSize="12" Margin="0,5,0,0" Opacity="0.9"/>
                        </StackPanel>
                    </materialDesign:Card>
                </StackPanel>
            </Border>

            <!-- Main Feed -->
            <Grid Grid.Column="1" Background="{DynamicResource MaterialDesignPaper}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="30,30,30,20">
                    <TextBlock Text="Notifications" FontSize="28" FontWeight="SemiBold"/>
                    <TextBlock Text="Your latest alerts synced directly from your phone." Foreground="{DynamicResource MaterialDesignBodyLight}" FontSize="14" Margin="0,5,0,0"/>
                </StackPanel>

                <ScrollViewer Grid.Row="1" Margin="30,0,30,20" VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="NotificationList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <materialDesign:Card Margin="0,0,0,12" Padding="20" UniformCornerRadius="12" Background="{DynamicResource MaterialDesignCardBackground}" materialDesign:ElevationAssist.Elevation="Dp2">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding AppName}" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource PrimaryHueMidBrush}" Margin="0,0,0,5"/>
                                            <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding Content}" TextWrapping="Wrap" Margin="0,5,0,0" Foreground="{DynamicResource MaterialDesignBodyLight}" FontSize="14"/>
                                        </StackPanel>
                                        <TextBlock Grid.Column="1" Text="{Binding Timestamp}" FontSize="12" Foreground="{DynamicResource MaterialDesignBodyLight}" VerticalAlignment="Top"/>
                                    </Grid>
                                </materialDesign:Card>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</Window>
