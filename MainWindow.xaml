<Window x:Class="CouchSync.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="CouchSync" Height="760" Width="1180"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        FontFamily="Segoe UI Variable Display"
        Background="#07111F">

    <Window.Resources>
        <LinearGradientBrush x:Key="WindowGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#07111F" Offset="0"/>
            <GradientStop Color="#102033" Offset="0.52"/>
            <GradientStop Color="#16304A" Offset="1"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="AccentGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#57D18D" Offset="0"/>
            <GradientStop Color="#2EA2A0" Offset="1"/>
        </LinearGradientBrush>
        <SolidColorBrush x:Key="CardBrush" Color="#1AFFFFFF"/>
        <SolidColorBrush x:Key="BorderBrushSoft" Color="#24FFFFFF"/>
        <SolidColorBrush x:Key="TextMuted" Color="#9AB2C8"/>
        <SolidColorBrush x:Key="WarningBrush" Color="#F7B955"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#57D18D"/>
    </Window.Resources>

    <Grid Background="{StaticResource WindowGradient}">
        <Border Background="#10000000" IsHitTestVisible="False"/>

        <Grid x:Name="PairingScreen" Visibility="Visible">
            <Grid Margin="56">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.1*"/>
                    <ColumnDefinition Width="0.9*"/>
                </Grid.ColumnDefinitions>

                <StackPanel VerticalAlignment="Center" Margin="0,0,28,0">
                    <TextBlock Text="CouchSync" FontSize="54" FontWeight="SemiBold" Foreground="White"/>
                    <TextBlock Text="A simpler phone-to-PC bridge for notifications." Margin="0,10,0,24" FontSize="20" Foreground="#D8E5F2"/>
                    <TextBlock Text="Pair once with the QR code. After that, the phone should reconnect without asking the user to save settings again." TextWrapping="Wrap" FontSize="16" Foreground="{StaticResource TextMuted}"/>

                    <Border Margin="0,28,0,0" Padding="24" CornerRadius="24" Background="{StaticResource CardBrush}" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="Why this flow exists" FontSize="18" FontWeight="SemiBold" Foreground="White"/>
                            <TextBlock Text="1. Scan the QR code on the Android app." Margin="0,14,0,0" Foreground="{StaticResource TextMuted}"/>
                            <TextBlock Text="2. Give notification access on Android." Margin="0,8,0,0" Foreground="{StaticResource TextMuted}"/>
                            <TextBlock Text="3. CouchSync keeps the session trusted and reconnects automatically when the phone comes back online." Margin="0,8,0,0" TextWrapping="Wrap" Foreground="{StaticResource TextMuted}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <Border Grid.Column="1" Padding="28" CornerRadius="30" Background="#14FFFFFF" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" VerticalAlignment="Center" MaxWidth="420">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="Scan to connect" FontSize="26" FontWeight="SemiBold" Foreground="White" HorizontalAlignment="Center"/>
                        <TextBlock Text="Open the Android app and scan this code." Margin="0,8,0,22" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center"/>

                        <Border Background="White" Padding="18" CornerRadius="22" HorizontalAlignment="Center">
                            <Image x:Name="QrCodeImage" Width="250" Height="250" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                        </Border>

                        <Border Margin="0,20,0,0" Background="#18000000" CornerRadius="18" Padding="18,14">
                            <StackPanel>
                                <TextBlock Text="Manual pairing code" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center"/>
                                <Border Margin="0,10,0,0" Padding="16,8" CornerRadius="14" Background="{StaticResource AccentGradient}">
                                    <TextBlock x:Name="PairingCodeText" Text="----" FontSize="30" FontWeight="Bold" Foreground="#07111F" HorizontalAlignment="Center"/>
                                </Border>
                            </StackPanel>
                        </Border>

                        <TextBlock x:Name="NetworkStatusText" Margin="0,20,0,0" Text="Listening for your phone..." TextAlignment="Center" TextWrapping="Wrap" Foreground="{StaticResource TextMuted}"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>

        <Grid x:Name="MainScreen" Visibility="Collapsed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="330"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#17000000" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="0,0,1,0">
                <StackPanel Margin="28">
                    <TextBlock Text="CouchSync" FontSize="34" FontWeight="SemiBold" Foreground="White"/>
                    <TextBlock Text="Trusted link" Margin="0,4,0,28" Foreground="{StaticResource TextMuted}"/>

                    <Border Padding="22" CornerRadius="24" Background="{StaticResource CardBrush}" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="Connected phone" Foreground="{StaticResource TextMuted}"/>
                            <TextBlock x:Name="ConnectedDeviceNameText" Margin="0,8,0,0" Text="Android phone" FontSize="24" FontWeight="SemiBold" Foreground="White" TextWrapping="Wrap"/>

                            <Border x:Name="ConnectionStatePill" Margin="0,18,0,0" Padding="12,8" CornerRadius="999" Background="#1FF7B955" HorizontalAlignment="Left">
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse x:Name="ConnectionStateDot" Width="10" Height="10" Fill="{StaticResource WarningBrush}" VerticalAlignment="Center"/>
                                    <TextBlock x:Name="ConnectionStateText" Margin="10,0,0,0" Text="Waiting for reconnect" Foreground="White" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>

                            <TextBlock x:Name="ConnectionDetailText" Margin="0,14,0,0" Text="Once the phone comes back online, CouchSync reconnects automatically." TextWrapping="Wrap" Foreground="{StaticResource TextMuted}"/>
                        </StackPanel>
                    </Border>

                    <Border Margin="0,22,0,0" Padding="20" CornerRadius="22" Background="#10000000" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="Quick actions" FontSize="16" FontWeight="SemiBold" Foreground="White"/>
                            <Button Style="{StaticResource MaterialDesignOutlinedButton}" Content="Clear All Alerts" Foreground="White" BorderBrush="#55FFFFFF" Margin="0,16,0,0" Height="46" materialDesign:ButtonAssist.CornerRadius="12" Click="ClearAlerts_Click"/>
                            <TextBlock Margin="0,14,0,0" Text="Clears the in-app feed, Windows Action Center, and the linked phone copy." TextWrapping="Wrap" Foreground="{StaticResource TextMuted}"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <Grid Grid.Column="1" Margin="36,34,36,28">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="Notifications" FontSize="38" FontWeight="SemiBold" Foreground="White"/>
                        <TextBlock Text="Mirrored from Android and kept in sync with Windows Action Center." Margin="0,8,0,0" Foreground="{StaticResource TextMuted}"/>
                    </StackPanel>

                    <Border Grid.Column="1" Background="#14000000" CornerRadius="20" Padding="18,12" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" VerticalAlignment="Center">
                        <StackPanel>
                            <TextBlock Text="Visible right now" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Right"/>
                            <TextBlock x:Name="NotificationCountText" Text="0 alerts" FontSize="24" FontWeight="SemiBold" Foreground="White" HorizontalAlignment="Right"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <Grid Grid.Row="1" Margin="0,26,0,0">
                    <Border Background="#12000000" CornerRadius="26" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1"/>
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="24">
                        <ItemsControl x:Name="NotificationList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="0,0,0,16" Padding="22" CornerRadius="20" Background="{StaticResource CardBrush}" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Grid.Column="0" Margin="0,0,20,0">
                                                <Border Background="#18000000" HorizontalAlignment="Left" CornerRadius="10" Padding="10,6" Margin="0,0,0,12">
                                                    <StackPanel Orientation="Horizontal">
                                                        <materialDesign:PackIcon Kind="BellBadgeOutline" Width="16" Height="16" Foreground="#57D18D" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                                        <TextBlock Text="{Binding AppName}" FontSize="12" FontWeight="SemiBold" Foreground="#D8E5F2" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </Border>
                                                <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="SemiBold" Foreground="White" TextWrapping="Wrap"/>
                                                <TextBlock Text="{Binding Content}" Margin="0,8,0,0" FontSize="15" Foreground="{StaticResource TextMuted}" TextWrapping="Wrap"/>
                                            </StackPanel>

                                            <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                                <TextBlock Text="{Binding Timestamp}" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Right"/>
                                                <Button Width="34" Height="34" Margin="0,14,0,0" Background="Transparent" BorderBrush="Transparent" Padding="0" HorizontalAlignment="Right" ToolTip="Dismiss" Click="DismissNotification_Click" Tag="{Binding}">
                                                    <materialDesign:PackIcon Kind="Close" Width="18" Height="18" Foreground="#D8E5F2"/>
                                                </Button>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <StackPanel x:Name="EmptyStatePanel" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <materialDesign:PackIcon Kind="BellSleepOutline" Width="64" Height="64" Foreground="#3BD8E5F2" HorizontalAlignment="Center"/>
                        <TextBlock Text="No active alerts" Margin="0,16,0,0" FontSize="24" FontWeight="SemiBold" Foreground="White" HorizontalAlignment="Center"/>
                        <TextBlock Text="New notifications will appear here as soon as the phone sends them." Margin="0,8,0,0" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center" TextAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>
