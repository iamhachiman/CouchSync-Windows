<Window x:Class="CouchSync.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:CouchSync"
        mc:Ignorable="d"
        Title="CouchSync" Height="750" Width="1100"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Background="#0f0c29">

    <Window.Resources>
        <!-- Gradients -->
        <LinearGradientBrush x:Key="BackgroundGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#0f0c29" Offset="0"/>
            <GradientStop Color="#302b63" Offset="0.5"/>
            <GradientStop Color="#24243e" Offset="1"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="CardGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#1AFFFFFF" Offset="0"/>
            <GradientStop Color="#05FFFFFF" Offset="1"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="AccentGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FF6A00" Offset="0"/>
            <GradientStop Color="#EE0979" Offset="1"/>
        </LinearGradientBrush>
    </Window.Resources>

    <Grid Background="{StaticResource BackgroundGradient}">
        <!-- Glass Effect Overlay -->
        <Border Background="#10000000" IsHitTestVisible="False" />

        <!-- PAIRING SCREEN -->
        <Grid x:Name="PairingScreen" Visibility="Visible">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="CouchSync" FontSize="48" FontWeight="Light" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,10">
                    <TextBlock.Effect>
                        <DropShadowEffect BlurRadius="20" ShadowDepth="0" Color="White" Opacity="0.5"/>
                    </TextBlock.Effect>
                </TextBlock>
                <TextBlock Text="Scan to sync your world." Foreground="#BBBBBB" FontSize="18" HorizontalAlignment="Center" Margin="0,0,0,40" FontWeight="SemiBold"/>
                
                <Border Padding="30" CornerRadius="24" Background="{StaticResource CardGradient}" BorderBrush="#33FFFFFF" BorderThickness="1" HorizontalAlignment="Center" Margin="0,0,0,40">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="40" Color="Black" Opacity="0.4" ShadowDepth="10"/>
                    </Border.Effect>
                    <Border Background="White" Padding="15" CornerRadius="12">
                        <Image x:Name="QrCodeImage" Width="260" Height="260" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                    </Border>
                </Border>

                <Border Background="{StaticResource CardGradient}" CornerRadius="10" Padding="20,10" Margin="0,0,0,30" HorizontalAlignment="Center" x:Name="PinPanel">
                    <StackPanel Orientation="Horizontal">
                        <StackPanel.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="10" />
                            </Style>
                        </StackPanel.Resources>
                        <TextBlock Text="MANUAL PIN :" FontSize="16" Foreground="#DDDDDD" VerticalAlignment="Center" Margin="0,0,15,0" FontWeight="Medium"/>
                        <Border Background="{StaticResource AccentGradient}" Padding="15,5" CornerRadius="8">
                            <TextBlock x:Name="PairingCodeText" Text="----" FontSize="26" FontWeight="Black" Foreground="White" VerticalAlignment="Center" />
                        </Border>
                    </StackPanel>
                </Border>
                
                <TextBlock x:Name="NetworkStatusText" Text="Initialize connecting parameters..." Margin="0,0,0,0" TextWrapping="Wrap" TextAlignment="Center" FontSize="14" Foreground="#FF9E9E9E" FontWeight="Medium"/>
            </StackPanel>
        </Grid>

        <!-- MAIN SCREEN -->
        <Grid x:Name="MainScreen" Visibility="Collapsed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <Border Grid.Column="0" Background="{StaticResource CardGradient}" BorderBrush="#22FFFFFF" BorderThickness="0,0,1,0">
                <StackPanel Margin="30">
                    <TextBlock Text="CouchSync" FontSize="32" FontWeight="Light" Foreground="White" Margin="0,0,0,40">
                        <TextBlock.Effect>
                            <DropShadowEffect BlurRadius="15" ShadowDepth="0" Color="White" Opacity="0.4"/>
                        </TextBlock.Effect>
                    </TextBlock>
                    
                    <Border Background="{StaticResource AccentGradient}" CornerRadius="16" Padding="20" Margin="0,0,0,30">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Color="#EE0979" Opacity="0.5"/>
                        </Border.Effect>
                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <materialDesign:PackIcon Kind="Cellphone" Width="30" Height="30" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="Connected Device" FontSize="12" Foreground="#EEFFFFFF" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="Android Remote" FontSize="18" FontWeight="Bold" Foreground="White" TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </Grid>
                            <Border Background="#33000000" CornerRadius="8" Padding="10" Margin="0,15,0,0">
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="8" Height="8" Fill="#00FF00" Margin="0,0,8,0" VerticalAlignment="Center">
                                        <Ellipse.Effect>
                                            <DropShadowEffect BlurRadius="5" Color="#00FF00" ShadowDepth="0"/>
                                        </Ellipse.Effect>
                                    </Ellipse>
                                    <TextBlock Text="Receiving Notifications" FontSize="12" Foreground="White" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Border>
                    
                    <TextBlock Text="QUICK ACTIONS" FontSize="12" FontWeight="Bold" Foreground="#88FFFFFF" Margin="0,0,0,10" />
                    <Button Style="{StaticResource MaterialDesignOutlinedButton}" Content="Clear All Alerts" Foreground="White" BorderBrush="#55FFFFFF" Margin="0,10" Height="45" materialDesign:ButtonAssist.CornerRadius="10" Click="ClearAlerts_Click"/>
                </StackPanel>
            </Border>

            <!-- Main Feed -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="40,40,40,20">
                    <TextBlock Text="Recent Alerts" FontSize="36" FontWeight="SemiBold" Foreground="White"/>
                    <TextBlock Text="Synced in real-time from your Android device." Foreground="#AABBBBBB" FontSize="15" Margin="0,5,0,0"/>
                </StackPanel>

                <ScrollViewer Grid.Row="1" Margin="40,10,40,30" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl x:Name="NotificationList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <!-- Notification Card -->
                                <Border Margin="0,0,0,15" Padding="25" CornerRadius="16" Background="#1AFFFFFF" BorderBrush="#22FFFFFF" BorderThickness="1">
                                    <!-- Add simple hover animation via Triggers if possible, but keep simple for now -->
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <!-- App Name & Content -->
                                        <StackPanel Grid.Column="0" Margin="0,0,20,0">
                                            <Border Background="#33FFFFFF" HorizontalAlignment="Left" CornerRadius="4" Padding="8,4" Margin="0,0,0,10">
                                                <StackPanel Orientation="Horizontal">
                                                    <materialDesign:PackIcon Kind="BellOutline" Width="14" Height="14" Foreground="#EE0979" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                                    <TextBlock Text="{Binding AppName}" FontSize="12" FontWeight="SemiBold" Foreground="#DDDDDD" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Border>
                                            <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="SemiBold" Foreground="White" TextWrapping="Wrap" Margin="0,0,0,6"/>
                                            <TextBlock Text="{Binding Content}" FontSize="15" Foreground="#BBBBBB" TextWrapping="Wrap"/>
                                        </StackPanel>
                                        <!-- Timestamp -->
                                        <TextBlock Grid.Column="1" Text="{Binding Timestamp}" FontSize="13" Foreground="#888888" VerticalAlignment="Top"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</Window>
